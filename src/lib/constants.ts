// Default reminder interval in minutes
export const DEFAULT_INTERVAL_MINUTES = 20;

// Default rest duration in seconds (Pomodoro technique - small tomato)
export const DEFAULT_REST_SECONDS = 20;

// Default big tomato rest duration in seconds (15 minutes)
export const DEFAULT_BIG_REST_SECONDS = 900;

// Small tomatoes per big tomato
export const SMALL_TOMATOES_PER_BIG = 4;

// Default reminder messages
export const DEFAULT_REMINDER_MESSAGES = [
  '让眼睛休息一下',
  '抬头看看远处',
  '闭上眼睛放松一下',
  '轻轻的向左看，然后向右看',
  '让眼睛做个深呼吸',
  '离开屏幕，伸展一下身体',
];

// Big rest messages
export const BIG_REST_MESSAGES = [
  '大番茄完成！休息一下吧',
  '干得好！长时间的休息',
  '4个小番茄结束！好好放松',
];

// Snooze duration options in minutes
export const SNOOZE_OPTIONS = [1, 3, 5, 10];

// Default sound settings
export const DEFAULT_SOUND_ENABLED = true;
